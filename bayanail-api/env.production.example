#
# Configuration de Production pour Moniteur1D API
# Copiez ce fichier en .env sur votre VPS et remplissez les valeurs
#

# ⚠️ IMPORTANT : Toujours "production" en production
NODE_ENV=production

# Port du serveur (généralement 3000 ou celui configuré dans votre reverse proxy)
PORT=3000

# ⚠️ IMPORTANT : URL du frontend en production (sans api.)
FRONTEND_URL="https://bayanail.com"

# --- Base de données (PostgreSQL Neon / Prisma) ---
DATABASE_URL="postgresql://USER:PASSWORD@host.neon.tech/dbname?sslmode=require&channel_binding=require"

# --- JWT (secrets longs et aléatoires - GÉNÉRER AVEC: npm run generate:secrets) ---
JWT_SECRET="change_me_generate_with_npm_run_generate_secrets"
JWT_REFRESH_SECRET="change_me_generate_with_npm_run_generate_secrets"

# --- Admin (création auto au démarrage si ADMIN_PASSWORD est défini) ---
ADMIN_EMAIL="admin@bayanail.com"
ADMIN_PASSWORD="change_me_strong_password_production"
ADMIN_FIRST_NAME="Admin"
ADMIN_LAST_NAME="System"

# --- Mot de passe uniforme (optionnel - remplace ADMIN_PASSWORD si défini) ---
# UNIFORM_PASSWORD="lounes92"

# --- Stripe (optionnel) ---
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Email (optionnel: Resend) ---
# RESEND_API_KEY="re_..."

# --- Cron / jobs (optionnel) ---
# CRON_TOKEN="change_me_random_token"

# --- Stockage (optionnel: Cloudflare R2). Si vide => stockage local en dev ---
# R2_ENDPOINT="https://<accountid>.r2.cloudflarestorage.com"
# R2_ACCESS_KEY_ID="..."
# R2_SECRET_ACCESS_KEY="..."
# R2_BUCKET_NAME="mon-bucket"
# R2_PUBLIC_URL="https://cdn.ton-domaine.com"

